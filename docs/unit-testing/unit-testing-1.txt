================================================================================
FINSIGHT — TEST SUITE CATALOG (unit-testing-1)
================================================================================
Tài liệu liệt kê chi tiết toàn bộ test tự động của nền tảng FinSight: mỗi lớp
test, loại (unit / integration), số lượng test và những gì nó kiểm chứng.

Phạm vi tính đến: 9 microservice (api-gateway, auth, user, transaction, budget,
dashboard, risk, notification, analytics) + frontend `web/` (mục 10).

TỔNG QUAN CON SỐ
--------------------------------------------------------------------------------
- Tổng số test method (@Test / @ParameterizedTest):  687
- Tổng số lớp test:                                  137 (+ vài lớp base/support)
- Trong đó:
    * Unit / component test (chạy KHÔNG cần Docker):   472
    * Integration test (cần Docker – Testcontainers):  215 (66 lớp *IntegrationTest)
- Frontend (Vitest, ngoài con số 687 ở trên):         166 test / 22 file
- Frontend E2E (Playwright, cần stack đang chạy):       9 test / 4 file (mục 11)
- Framework: JUnit 5 (Jupiter) + Mockito + AssertJ; frontend dùng Vitest +
  React Testing Library (jsdom) và Playwright cho E2E trình duyệt.
- Integration test dùng Testcontainers (MySQL 8, Kafka KRaft, Redis) và WireMock
  (giả lập HTTP upstream), Awaitility (chờ bất đồng bộ).

PHÂN BIỆT UNIT vs INTEGRATION
--------------------------------------------------------------------------------
- UNIT test: kiểm thử một lớp logic ở trạng thái CÔ LẬP, phụ thuộc được mock bằng
  Mockito. Không khởi động Spring context, không cần Docker. Rất nhanh.
- COMPONENT/SLICE test: khởi động một phần Spring (ví dụ @WebMvcTest cho controller,
  hoặc gateway + WireMock), không dùng Testcontainers.
- INTEGRATION test: khởi động Spring đầy đủ + hạ tầng thật qua Testcontainers
  (MySQL/Kafka/Redis). Tên lớp kết thúc bằng "IntegrationTest".

CÁCH CHẠY
--------------------------------------------------------------------------------
- Mỗi service là 1 project Maven độc lập (không có aggregator pom).
- Chạy toàn bộ (unit + integration) cho 1 service:
      cd services/<service> && mvnw.cmd test            (Windows)
      cd services/<service> && ./mvnw verify            (Bash/Linux)
  ("verify" chạy cả unit lẫn integration vì integration test chạy qua Surefire.)
- Chạy 1 lớp:    mvnw.cmd test -Dtest=CurrencyValidatorTest
- Chạy 1 method: mvnw.cmd test -Dtest=CurrencyValidatorTest#rejectsUnknownCodes
- CI: GitHub Actions (.github/workflows/ci.yml) chạy `mvn verify` cho cả 9 service
  trên ma trận (JDK 21), Docker sẵn có nên Testcontainers hoạt động.

Quy ước chung của test:
- userId luôn lấy từ JWT (claim), không bao giờ từ request body → nhiều test kiểm
  chứng "tenant isolation" (user A không thấy dữ liệu user B).
- Soft delete: test kiểm chứng bản ghi đã xóa mềm bị ẩn khỏi truy vấn.
- Idempotency inbox (processed_events): test kiểm chứng sự kiện trùng chỉ xử lý 1 lần.
- Envelope API chuẩn { success, data, meta } / { success, error{code,message} }.


================================================================================
1) API-GATEWAY  (cổng vào, xác thực JWT ở biên, định tuyến)
================================================================================

[UNIT] JwtKeyResolverTest — 9 test
  Giải mã/khám phá khóa RS256 qua JWK Set (JWKS) + xoay khóa. Dùng 1 HTTP server
  JWKS cục bộ để mô phỏng auth-service.
   - withoutAJwksUri_theConfiguredKeyIsUsedAndNothingIsFetched
   - aKeyKnownOnlyToTheJwksVerifies_provingTheDocumentIsUnderstood
   - theFetchedSetReplacesTheBootstrapKeyRatherThanAddingToIt
   - duringTheOverlapWindow_bothTheOutgoingAndIncomingKeyVerify
   - whenTheJwksIsUnreachable_theBootstrapKeyStillVerifies
   - aFailedRefreshKeepsTheKeysAlreadyLearned_andIsCounted
   - unknownKidsCannotStampedeAuthService (chống DoS khuếch đại)
   - aTokenWithNoKidUsesTheBootstrapKey
   - anUnknownKeyIsRejectedRatherThanFallingBack

[UNIT] TokenRevocationCheckerTest — 6 test
  Danh sách thu hồi token qua Redis (logout/ban/đổi role vô hiệu token ngay).
   - noCutoffForUser_isNotRevoked
   - tokenIssuedBeforeCutoff_isRevoked
   - tokenIssuedAtOrAfterCutoff_survives
   - redisDown_failsOpenAndCounts (Redis lỗi → fail-open, có đếm metric)
   - malformedCutoff_failsOpenAndCounts
   - claimsWithoutUserIdOrIssuedAt_areNotRevoked_andNeverHitRedis

[COMPONENT] GatewayAuthTest — 11 test  (Spring context + WireMock, không Testcontainers)
  Thực thi xác thực ở gateway trên mọi tình huống token.
   - protectedRoute_noToken_returnsUnauthenticated
   - protectedRoute_malformedAuthHeader_returnsUnauthenticated
   - protectedRoute_expiredToken_returnsTokenExpired
   - protectedRoute_badSignature_returnsTokenInvalid
   - protectedRoute_wrongAlgorithm_returnsTokenInvalid
   - protectedRoute_wrongIssuer_returnsTokenInvalid
   - protectedRoute_wrongAudience_returnsTokenInvalid
   - revokedTokenIsRejected
   - protectedRoute_validToken_passesAuthAndForwards
   - publicRoute_noToken_bypassesAuthAndForwards
   - authMe_isProtected_noToken_returnsUnauthenticated

[COMPONENT] GatewayRoutingTest — 3 test
   - unknownPrefixReturnsRouteNotFound
   - knownPrefixWithValidTokenAndDeadBackendReturnsServiceUnavailable
   - oversizedBodyReturnsPayloadTooLarge (chặn body > 2 MiB → 413)

[UNIT] RateLimiterTest — 8 test  (giới hạn tần suất ở gateway, Redis + Lua)
   - underTheLimit_isAllowed
   - overTheLimit_isRejectedWithTheRemainingWindowAsRetryAfter
   - theTwoScopesHaveIndependentLimits (ẩn danh theo IP vs đã đăng nhập theo user)
   - aMissingTtlFallsBackToTheFullWindow
   - redisDown_failsOpenAndCounts (Redis chết thì cho qua, không chặn người dùng)
   - anUnexpectedScriptReply_failsOpenAndCounts
   - disabled_allowsWithoutTouchingRedis
   - aBlankIdentityIsNotCounted

[UNIT] ClientIpResolverTest — 5 test  (lấy đúng IP thật của client sau Cloudflare)
   - cloudflareHeaderWins
   - withoutCloudflare_theLeftmostForwardedEntryIsTheClient
   - aSingleForwardedEntryIsUsedAsIs
   - blankHeadersAreIgnored
   - noForwardedHeaders_fallsBackToTheSocketAddress

[UNIT] CorrelationIdFilterTest — 4 test  (bộ chuẩn correlation id — xem GHI CHÚ CUỐI)

[COMPONENT] PrometheusEndpointTest — 1 test
   - prometheusEndpointIsReachableAndCarriesJvmMetrics

[INTEGRATION] RevocationDenylistIntegrationTest — 3 test  (Redis Testcontainer)
   - tokenIsAcceptedWhileNoCutoffExists
   - sameTokenIsRejectedOnceTheUserIsRevoked
   - aTokenMintedAfterTheRevocationWorksAgain


================================================================================
2) AUTH-SERVICE  (đăng ký/đăng nhập, JWT RS256, refresh, khóa tài khoản)
================================================================================

[UNIT] JwtServiceTest — 5 test
  Xác thực token cục bộ bằng public key (pin thuật toán + iss/aud).
   - validTokenAccepted
   - wrongIssuerRejected
   - wrongAudienceRejected
   - wrongAlgorithmRejected
   - expiredTokenRejected

[UNIT] JwtKeyRotationTest — 5 test
  Xoay khóa RS256 không downtime; kid = RFC 7638 thumbprint.
   - theKidIsDerivedFromTheKeyItself_soIssuerAndValidatorAgreeWithoutConfig
   - midRotation_theOutgoingKeyStillVerifiesButNoLongerSigns
   - afterTheWindowCloses_theRetiredKeyIsRejected
   - theJwksAdvertisesEveryKeyThatVerifies
   - blankAndDuplicatePreviousKeysAreTolerated

[INTEGRATION] AuthApiIntegrationTest — 8 test  (MySQL Testcontainer)
   - registerThenLoginReturnsTokens
   - registerDuplicateEmailIsConflict
   - registerDuplicateUsernameIsConflict
   - registerInvalidEmailIsValidationError
   - registerShortPasswordIsValidationError
   - loginWrongPasswordIsUnauthorized
   - loginUnknownEmailIsUnauthorized
   - malformedJsonIsBadRequest

[INTEGRATION] RefreshTokenIntegrationTest — 4 test
   - refreshReturnsNewTokens
   - rotationInvalidatesPreviousRefreshToken
   - logoutRevokesRefreshToken
   - refreshWithUnknownTokenIsUnauthorized

[INTEGRATION] LoginObservabilityIntegrationTest — 4 test  (MySQL + Redis thật)
   Trước đây một lần đăng nhập KHÔNG để lại dấu vết nào đọc được: bằng chứng duy nhất là refresh
   token trong Redis, hết hạn là mất. Hỏi "ngày 19 có ai đăng nhập không?" thì không gì trên box
   trả lời được. Phải là integration test chứ không mock: cột phải sống qua một commit thật, và
   counter phải được đăng ký trong context thật.
   - recordsASuccessfulLogin  (counter +1 VÀ `last_login_at` được đóng dấu)
   - recordsAFailedLogin  (counter +1 nhưng KHÔNG đóng dấu — đóng dấu ở đây sẽ khiến một đợt
     brute-force trông như làn sóng người dùng hoạt động)
   - doesNotLeakAccountExistenceThroughMetrics  (email lạ và sai mật khẩu dùng CHUNG một counter;
     tách ra là rò rỉ qua Prometheus đúng thứ mà API cố tình không tiết lộ)
   - registersAllOutcomesUpFront  (counter chưa từng bắn thì VẮNG MẶT khỏi /actuator/prometheus,
     và một series vắng mặt hiện là "no data" — không phân biệt được với service chết)

[INTEGRATION] LoginLockoutIntegrationTest — 2 test
   - lockoutAfterMaxFailedAttemptsBlocksEvenCorrectPassword
   - successfulLoginResetsTheFailureCounter

[INTEGRATION] DisabledAccountIntegrationTest — 1 test
   - disabledUserCannotLogin

[INTEGRATION] PasswordEncodingIntegrationTest — 2 test
   - newRegistrationsAreStoredWithBcryptPrefix
   - legacyBareBcryptHashStillAuthenticates

[INTEGRATION] JwtClaimsIntegrationTest — 2 test
   - issuedTokenCarriesIssuerAndAudience
   - tokenWithoutIssuerOrAudienceIsRejected

[INTEGRATION] JwtSecurityIntegrationTest — 4 test
   - meWithoutTokenIsUnauthorized
   - meWithValidTokenReturnsEmail
   - meWithForgedSignatureIsUnauthorized
   - meWithExpiredTokenIsUnauthorized

[INTEGRATION] JwksEndpointIntegrationTest — 4 test
   - jwksIsPublic_andServesTheSigningKey
   - issuedTokensCarryAKidThatResolvesInTheJwks
   - aTokenVerifiesWithTheKeyTakenFromTheJwks
   - theJwksNeverLeaksPrivateKeyMaterial (bảo vệ tuyệt đối private key)

[UNIT] CorrelationIdFilterTest — 4 test  (bộ chuẩn correlation id — xem GHI CHÚ CUỐI)

[INTEGRATION] TokenRevocationIntegrationTest — 3 test  (Redis)
   - loginAloneRevokesNothing
   - logoutWritesACutoffThatCoversTheAccessTokenJustIssued
   - cutoffExpiresWithTheAccessTokenLifetime

[INTEGRATION] OpenApiDocsIntegrationTest — 1 test   (apiDocsArePubliclyReachable)
[INTEGRATION] PrometheusEndpointIntegrationTest — 1 test
[CONTEXT]     AuthServiceApplicationTests — 1 test  (context load)


================================================================================
3) USER-SERVICE  (hồ sơ người dùng; có endpoint gRPC nội bộ)
================================================================================

[UNIT] UserProfileServiceTest — 7 test
   - createProfile_success_returnsResponse
   - createProfile_duplicateProfile_throwsConflict
   - getMyProfile_success_returnsProfile
   - getMyProfile_notFound_throwsException
   - updateProfile_partialFields_onlyUpdatesNonNull
   - updateProfile_allFields_updatesAll
   - updateProfile_notFound_throwsException

[COMPONENT] UserProfileControllerTest — 15 test  (@WebMvcTest slice — controller + validation)
   - createProfile_success_returns201
   - createProfile_allFields_mapsCorrectly
   - getMyProfile_success_returns200
   - getMyProfile_notFound_returns404
   - updateProfile_success_returns200
   - updateProfile_notFound_returns404
   - createProfile_missingFullName_returns400
   - createProfile_blankFullName_returns400
   - createProfile_invalidPhone_returns400
   - createProfile_futureDateOfBirth_returns400
   - updateProfile_invalidPhone_returns400
   - request_withoutToken_returns401
   - postRequest_withoutToken_returns401
   - request_withInvalidToken_returns401
   - request_withValidAuth_passesThrough

[UNIT] JwtServiceTest — 5 test  (validTokenAccepted / wrongIssuer / wrongAudience /
        wrongAlgorithm / expiredToken — cùng bộ như các service khác)

[UNIT] CorrelationIdFilterTest — 4 test  (bộ chuẩn correlation id — xem GHI CHÚ CUỐI)

[INTEGRATION] UserProfileApiIntegrationTest — 5 test  (MySQL)
   - createThenGetProfile_persistsToMySql
   - updateProfile_partialUpdate_keepsOtherFields
   - createDuplicateProfile_returns409
   - getMissingProfile_returns404
   - request_withoutToken_returns401

[INTEGRATION] UserProfileApiSecurityIntegrationTest — 5 test
   - rejectsRequestWithoutJwt
   - rejectsJwtWithInvalidSignature
   - rejectsExpiredJwt
   - allowsValidJwtAndScopesProfileToTokenUserId
   - userCannotSeeAnotherUsersProfile (cô lập tenant)

[INTEGRATION] UserProfileGrpcIntegrationTest — 5 test  (gRPC server + MySQL)
   - getMyProfile_returnsPersistedProfile_forValidToken
   - getMyProfile_returnsNotFound_whenNoProfileYet
   - getMyProfile_rejectsMissingToken_withUnauthenticated
   - getMyProfile_rejectsForgedToken_withUnauthenticated
   - getMyProfile_rejectsExpiredToken_withUnauthenticated

[INTEGRATION] OpenApiDocsIntegrationTest — 1 test
[INTEGRATION] PrometheusEndpointIntegrationTest — 1 test
[CONTEXT]     UserServiceApplicationTests — 1 test


================================================================================
4) TRANSACTION-SERVICE  (giao dịch, danh mục, ví, tổng hợp, game, outbox, xuất CSV)
================================================================================

[UNIT] TransactionCsvWriterTest — 7 test  (xuất CSV — bản đối xứng của nhập sao kê)
   - writesTheHeaderEvenWithNoRows
   - expenseIsNegativeAndIncomeIsPositive (chế độ mặc định của trang import đọc số âm là
     tiền ra, nên đây chính là thứ khiến file xuất ra nhập lại được mà không phải ánh xạ
     cột nào; số cũng bị cắt đuôi 0 vì cột là DECIMAL(19,4) — nếu không, một khoản VND sẽ
     ra 250000.0000)
   - transferLeavesTheWalletSoItIsNegativeToo
   - anUnknownCategoryIsBlankRatherThanANumber (một số trần sẽ đọc như cột tiền thứ hai)
   - quotesADescriptionContainingADelimiterOrAQuote
   - neutralisesADescriptionASpreadsheetWouldRunAsAFormula
     (mô tả là chữ người dùng tự gõ; để nguyên thì "=1+1" thành công thức ngay khi mở file
      bằng Excel — lỗ hổng CSV injection mà mọi chức năng xuất file đều phải xử lý)
   - aMissingDescriptionIsAnEmptyField

[UNIT] CurrencyValidatorTest — 3 test
   - acceptsValidIso4217Codes
   - rejectsUnknownCodes
   - treatsNullAsValidSoNotNullOwnsPresence

[UNIT] RouletteTest — 7 test  (toán học game American Roulette, server-authoritative)
   - wheelComposition            (đúng thành phần bánh xe)
   - coloursAlternate            (màu xen kẽ)
   - payoutsEncodeTheHouseEdge   (payout phản ánh lợi thế nhà cái)
   - legalPositionCount          (161 vị trí cược hợp lệ)
   - illegalPositionsRejected
   - payoutArithmetic            (mọi payout không phải basket = (36−n)/n)
   - spinStaysOnTheWheel

[UNIT] OutboxRelayTest — 7 test  (transactional outbox → Kafka đáng tin cậy)
   - publishesEveryRowThenDeletesThemInOneBatch
   - stopsAtFirstFailure_deletesOnlyRowsPublishedBefore_andNeverReordersAhead
   - disabled_doesNotTouchTheDatabaseOrBroker
   - emptyBatch_sendsNothingAndDeletesNothing
   - replaysTheStoredCorrelationIdOntoTheRecordHeader
   - addsNoHeaderWhenTheRowHasNoCorrelationId
   - restoresTheCorrelationIdInTheMdcWhilePublishing_andClearsItAfterwards

[UNIT] OutboxWriterTest — 3 test  (ghi outbox trong cùng transaction nghiệp vụ)
   - serializesThePayloadAndCarriesTheRoutingFieldsThrough
   - capturesTheRequestsCorrelationIdFromTheMdc
   - storesNoCorrelationIdWhenTheEventIsProducedOutsideARequest

[UNIT] JwtServiceTest — 5 test  (bộ chuẩn validTokenAccepted/wrongIssuer/...)
[UNIT] CorrelationIdFilterTest — 4 test  (bộ chuẩn correlation id — xem GHI CHÚ CUỐI)

[INTEGRATION] TransactionApiCrudIntegrationTest — 12 test  (MySQL)
   - createReturnsCreatedTransaction
   - getByIdReturnsTransaction
   - updateAppliesPartialChanges
   - softDeleteHidesTransactionFromSubsequentReads
   - createsTransferAndMovesTheBalanceBetweenWallets
   - rejectsTransferMissingDestinationWallet
   - rejectsTransferToSameWallet
   - rejectsTransactionReferencingAnotherUsersWallet
   - rejectsNonPositiveAmountWithStructuredError
   - rejectsInvalidCurrencyWithStructuredError
   - rejectsMissingTransactionDateWithStructuredError
   - rejectsUnknownCategoryWithStructuredError

[INTEGRATION] TransactionIdempotencyIntegrationTest — 4 test  (gửi lại một lệnh ghi, MySQL thật)
   Nền tảng của hàng đợi offline: hàng đợi KHÔNG thể biết một request lỗi giữa chừng đã được áp
   hay chưa, nên gửi lại phải an toàn. Bảo đảm thật nằm ở **unique index
   `(user_id, client_request_id)`** (`V14`) — kiểm tra trong service chỉ làm ca tuần tự rẻ đi,
   và chỉ test trên MySQL thật mới chạm tới cái index đó.
   - replayIsIdempotent  (lần 2 trả về ĐÚNG id lần 1, và sổ chỉ có 1 dòng)
   - tokensAreScopedPerUser  (2 client tự sinh token, có thể trùng chuỗi — lệnh của người này
     không bao giờ được rơi vào giao dịch của người kia)
   - staysUnchangedWithoutAToken  (2 ly cà phê gõ tay giống hệt nhau là 2 ly; gộp lại là app tự
     cho rằng mình biết rõ hơn người vừa gõ)
   - blankTokenDoesNotClaimTheIndex  (token rỗng phải coi như KHÔNG có; lưu "" thì nó chiếm ô
     unique và lần ghi rỗng kế tiếp của user sẽ rơi vào giao dịch cũ)

[INTEGRATION] TransactionImportIntegrationTest — 9 test  (nhập sao kê + chống trùng)
   - importWritesEveryRow
   - reimportingTheSameStatementSkipsEveryRow
   - repeatedRowWithinOneFileIsImportedOnce
   - amountAndDescriptionAreNormalisedBeforeComparing
   - abadRowIsReportedWithoutStoppingTheRest
   - deletingAnImportedRowAllowsItToBeImportedAgain
   - anIdenticalRowFromAnotherUserIsNotADuplicate
   - anEmptyImportIsRejected
   - importRequiresAuthentication

[INTEGRATION] TransactionExportIntegrationTest — 5 test  (xuất CSV qua HTTP)
   - exportsTheUsersTransactionsAsADownloadableCsv (Content-Type text/csv;charset=UTF-8 +
     Content-Disposition attachment; thứ tự mới nhất trước, khớp endpoint list)
   - appliesTheSameFiltersAsTheListEndpoint
   - neverExportsAnotherUsersTransactions
   - excludesADeletedTransaction (xoá mềm đã biến mất khỏi list thì phải biến mất khỏi file)
   - exportRequiresAuthentication

[INTEGRATION] VietnameseTextIntegrationTest — 5 test  (tiếng Việt đi trọn vòng, so BYTE)
   Mojibake không nhìn thấy được từ bên trong ứng dụng: mỗi tầng trao cho tầng sau một
   String, nên description đọc lại vẫn đúng ngay cả khi byte dưới đĩa đã hỏng — chỉ trình
   duyệt và bảng tính mới lộ ra. Vì vậy lớp này khẳng định BYTE (HEX() lấy thẳng từ MySQL,
   và body thô của response) so với hằng số viết bằng ASCII.
   - storesAVietnameseDescriptionAsTheUtf8BytesTheClientSent (HEX + CHAR_LENGTH + LENGTH)
   - storesAVietnameseWalletNameAsTheUtf8BytesTheClientSent
   - returnsAVietnameseDescriptionAsUtf8OverJson (khẳng định trước khi decode)
   - exportsAVietnameseDescriptionAsUtf8Csv (charset trên Content-Type, thân file không BOM)
   - everyTextColumnInTheSchemaIsUtf8mb4 (chặn migration sau này thêm cột sai charset)
   Chuỗi mẫu cố tình phủ cả 4 độ rộng UTF-8, kể cả một emoji 4 byte — thứ chỉ lưu được vì
   schema là utf8mb4 chứ không phải utf8mb3.

[INTEGRATION] TransactionApiFilterIntegrationTest — 7 test
   - defaultSortingIsTransactionDateDesc
   - filterByTypeReturnsOnlyMatchingType
   - filterByDateRangeIsInclusive
   - combinedFiltersAreAnded
   - paginationIsDeterministicAcrossPagesEvenWithTiedDates
   - limitAboveMaximumReturnsStructuredValidationError
   - pageBelowOneReturnsStructuredValidationError

[INTEGRATION] TransactionApiSecurityIntegrationTest — 5 test
   - rejectsRequestWithoutJwt / rejectsJwtWithInvalidSignature / rejectsExpiredJwt
   - allowsValidJwtAndScopesDataToTokenUserId
   - userCannotAccessAnotherUsersTransaction

[INTEGRATION] TransactionPersistenceIntegrationTest — 3 test
   - flywaySeedsTheDefaultCategories
   - savesAndFetchesTransactionWithJsonMetadata (cột JSON MySQL)
   - softDeletedTransactionIsHiddenFromTheOwnerQuery

[INTEGRATION] TransactionSummaryIntegrationTest — 5 test
   - monthlySummaryReturnsIncomeExpenseAndBalance
   - monthlySummaryIsZeroedWhenNoTransactions
   - categoryBreakdownIsGroupedAndOrderedByTotalDesc
   - dailyTrendAggregatesPerDayAscending
   - summariesAreScopedToTheAuthenticatedUser

[INTEGRATION] CategoryApiIntegrationTest — 8 test
   - listIncludesSeededSystemCategories
   - createReturnsNonSystemCategoryWithGeneratedId
   - updateAppliesPartialChangesAndKeepsTypeWhenAbsent
   - deleteUserCreatedCategorySucceeds
   - cannotDeleteSystemCategory
   - cannotDeleteCategoryReferencedByTransactions
   - updateMissingCategoryReturns404
   - createWithBlankNameReturnsValidationError

[INTEGRATION] WalletApiIntegrationTest — 5 test
   - listReturnsOnlyTheUsersWallets
   - incomeCreditsAndExpenseDebitsTheWallet (số dư cập nhật nguyên tử)
   - rejectsTransactionWhoseCurrencyDiffersFromTheWallet
   - deletingTransactionRestoresTheWalletBalance
   - emptyWalletCanBeDeletedButANonEmptyOneCannot

[INTEGRATION] SchemaIndexIntegrationTest — 3 test
   - flywayMigrationsAppliedSuccessfully
   - mandatoryIndexesExistOnTransactions
   - userScopedFilteredQueryExecutesAgainstMigratedSchema

[INTEGRATION] TransactionEventPublishingIntegrationTest — 1 test  (Kafka)
   - publishesTransactionCreatedEventAfterCommit

[INTEGRATION] PrometheusEndpointIntegrationTest — 1 test


================================================================================
5) BUDGET-SERVICE  (định nghĩa ngân sách + tiêu thụ từ sự kiện Kafka)
================================================================================

[UNIT] BudgetServiceImplTest — 20 test
   - createPersistsBudgetScopedToTokenUserId
   - createRejectsNonPositiveLimit
   - createRejectsEndBeforeStart
   - createRejectsDuplicateActiveBudget
   - getByIdThrowsWhenNotOwned
   - updateAppliesOnlyNonNullFieldsAndKeepsSlotCheckOff
   - updateChecksDuplicateWhenSlotChanges
   - applyExpenseRecordsInboxRowAndIncrementsBudgets
   - applyExpenseSkipsDuplicateEventWithoutTouchingBudgets
   - applyDeleteReversesTheExpenseWithANegatedAmount
   - applyDeleteSkipsDuplicateEventWithoutTouchingBudgets
   - applyUpdateReversesOldSlotAndAppliesNewSlotUnderOneInboxRow
   - applyUpdateWithNullApplyOnlyReversesTheOldSlot
   - applyUpdateSkipsDuplicateEventWithoutTouchingBudgets
   - deleteSoftDeletesOwnedBudget
   Nhóm BudgetExceeded — "bắn đúng một lần lúc vượt" (5 test):
   - applyExpensePublishesBudgetExceededOnTheEventThatCrossesTheLimit
   - applyExpenseStaysSilentOnceTheBudgetIsAlreadyOver
     (nếu thiếu: mọi khoản chi sau khi vượt đều đẻ thêm 1 push + 1 email + 1 dòng chuông)
   - applyExpenseStaysSilentWhileTheBudgetIsStillWithinItsLimit
   - reachingTheLimitExactlyIsNotExceedingIt (tiêu đúng hạn mức không phải là vượt)
   - reversingAnExpenseNeverAnnouncesAnExceededBudget
     (applyDelete phủ định số tiền; sửa giao dịch không phải là chi mới)

[UNIT] TransactionEventConsumerTest — 12 test  (idempotency + khớp sự kiện)
   - appliedExpenseCountsAsProcessed
   - duplicateExpenseCountsAsDuplicate
   - nonExpenseIsIgnoredWithoutReachingTheService
   - missingEventIdIsIgnoredWithoutReachingTheService
   - missingDateIsIgnoredWithoutReachingTheService
   - unparseableDateIsIgnoredWithoutReachingTheService
   - updatedBetweenTwoExpensesReversesOldSlotAndAppliesNewSlot
   - updatedFromExpenseToIncomeReversesOnlyTheOldSlot
   - updatedBetweenTwoNonExpensesIsIgnoredWithoutReachingTheService
   - updatedMissingEventIdIsIgnoredWithoutReachingTheService
   - deletedExpenseIsReversedAndCountsAsProcessed
   - deletedNonExpenseIsIgnoredWithoutReachingTheService
   - deletedMissingEventIdIsIgnoredWithoutReachingTheService

[UNIT] CurrencyValidatorTest — 3 test  (acceptsValidIso4217Codes / rejectsUnknownCodes /
        treatsNullAsValidSoNotNullOwnsPresence)

[UNIT] BudgetEventListenerTest — 2 test
   - publishesWhenKafkaEnabled
   - skipsPublishWhenKafkaDisabled

[UNIT] KafkaBudgetEventPublisherTest — 4 test  (định tuyến + correlation id lên header)
   - sendsToTheConfiguredTopicKeyedByUserId
   - sendsBudgetExceededToItsOwnTopicStillKeyedByUserId
     (topic khác nhưng cùng key userId → cảnh báo của một người vẫn giữ thứ tự
      với các sự kiện ngân sách khác của chính họ, không đua nhau qua partition)
   - carriesTheCurrentCorrelationIdAsARecordHeader
   - addsNoHeaderWhenThereIsNoCorrelationIdInScope

[UNIT] KafkaConsumerConfigTest — 2 test  (metric thất bại consumer)
   - counterRegisteredAtZeroBeforeAnyFailure
   - eachRecoveredRecordIncrementsFailedOnce

[UNIT] JwtServiceTest — 5 test  (bộ chuẩn)
[UNIT] CorrelationIdFilterTest — 4 test  (bộ chuẩn correlation id — xem GHI CHÚ CUỐI)
[UNIT] CorrelationIdRecordInterceptorTest — 5 test  (correlation id qua Kafka — xem GHI CHÚ CUỐI)

[INTEGRATION] BudgetApiCrudIntegrationTest — 10 test  (MySQL)
   - createReturnsCreatedBudget / getByIdReturnsBudget / updateAppliesPartialChanges
   - softDeleteHidesBudgetFromSubsequentReads
   - duplicateActiveBudgetReturnsConflict
   - recreatingAfterSoftDeleteIsAllowed
   - rejectsNonPositiveLimitWithStructuredError
   - rejectsInvalidCurrencyWithStructuredError
   - rejectsMissingStartDateWithStructuredError
   - rejectsEndDateBeforeStartDateWithStructuredError

[INTEGRATION] BudgetApiFilterIntegrationTest — 8 test
   - defaultSortingIsStartDateDesc / filterByCategory / filterByPeriodType
   - filterByActiveOnReturnsBudgetsCoveringTheDate
   - combinedFiltersAreAnded
   - paginationIsDeterministicAcrossPagesEvenWithTiedDates
   - limitAboveMaximumReturnsStructuredValidationError
   - pageBelowOneReturnsStructuredValidationError

[INTEGRATION] BudgetApiSecurityIntegrationTest — 5 test
   - rejectsRequestWithoutJwt / rejectsJwtWithInvalidSignature / rejectsExpiredJwt
   - allowsValidJwtAndScopesDataToTokenUserId
   - userCannotAccessAnotherUsersBudget

[INTEGRATION] BudgetPersistenceIntegrationTest — 3 test
   - savesAndFetchesBudget
   - softDeletedBudgetIsHiddenFromTheOwnerQuery
   - duplicateGuardIgnoresSoftDeletedRows

[INTEGRATION] BudgetUtilizationConsumerIntegrationTest — 5 test  (Kafka + MySQL)
   - expenseEventIncrementsMatchingBudget
   - expenseEventIncrementsAllOverlappingBudgets
   - nonMatchingEventsContributeNothing
   - duplicateEventIdIsCountedExactlyOnce
   - consumerLagMetricIsExposedForPrometheus

[INTEGRATION] BudgetReversalConsumerIntegrationTest — 4 test  (Kafka + MySQL)
   - deleteEventReversesSpentAmount
   - updateEventReappliesWithTheNewAmount
   - updateEventMovesSpendBetweenCategoryBudgets
   - duplicateDeleteIsAppliedExactlyOnce

[INTEGRATION] SchemaIndexIntegrationTest — 3 test
   - flywayMigrationsAppliedSuccessfully
   - mandatoryIndexesExistOnBudgets
   - userScopedFilteredQueryExecutesAgainstMigratedSchema

[INTEGRATION] OpenApiDocsIntegrationTest — 1 test
[INTEGRATION] PrometheusEndpointIntegrationTest — 1 test


================================================================================
6) DASHBOARD-SERVICE  (BFF tổng hợp; không có DB riêng)
================================================================================

[UNIT] DashboardServiceTest — 30 test  (lớp test lớn nhất — logic tổng hợp BFF)
  Ngân sách/tiến độ:
   - budgetProgress_joinsLimitWithExpenseSpend
   - budgetProgress_ignoresIncomeRows_andZeroesUnspentBudgets
   - budgetProgress_defaultsWindowToCurrentMonth_whenDatesNull
   - budgetProgress_roundsPercentToTwoDecimals
  Giao dịch gần đây:
   - recentTransactions_defaultLimit_callsClientWithFive_andMapsFields
   - recentTransactions_customLimit_passedThrough
   - recentTransactions_limitOver100_clampedTo100
   - recentTransactions_limitZeroOrNegative_clampedToOne
   - recentTransactions_emptyResponse_returnsEmptyList
  Top danh mục:
   - topCategories_filtersExpense_sortsDesc_andLimits
   - topCategories_limitLargerThanCount_returnsAllSorted
   - topCategories_noExpense_returnsEmptyList
   - topCategories_zeroOrNegativeLimit_returnsEmptyList_withoutError
  Tổng hợp KPI:
   - summary_computesIncomeExpenseBalanceAndUtilization
   - summary_noBudgets_utilizationIsZero_butKpisStillComputed
   - summary_defaultsWindowToCurrentMonth_whenDatesNull
  Xu hướng (trend):
   - trend_bucketsDailyPointsIntoMonths_sumsAndComputesBalance
   - trend_emptyUpstream_returnsEmptyList
   - trend_defaultsWindowToCurrentMonth_whenDatesNull
   - trend_monthBoundary_splitsAdjacentDaysAndOutputsChronologically_evenIfUpstreamUnordered
   - trend_dayGranularity_passesThroughPerDayWithIsoDatePeriod
   - trend_unknownGranularity_fallsBackToMonth
   - trend_nullMoneyFields_treatedAsZero
   - trend_aggregationPrecision_isExact
  Composite / overview:
   - composite_assemblesAllBlocks_andFetchesEachUpstreamExactlyOnce
   - composite_defaultsWindowToCurrentMonth_whenDatesNull
   - composite_emptyData_returnsEmptyBlocks_andZeroSummary
   - composite_nullProfile_tolerated_otherBlocksStillBuilt
   - overview_composesProfileSummaryAndBudgets
   - overview_toleratesMissingProfile

[UNIT] UpstreamCallsTest — 3 test  (Resilience4j: circuit breaker + retry)
   - retriesTransientFailureThenSucceeds
   - doesNotRetryClientError_andWrapsAsUpstreamException
   - opensBreakerAfterRepeatedFailures_thenShortCircuits

[UNIT] BudgetClientTest — 2 test  (listBudgets_unwrapsEnvelope_andRelaysJwt /
        listBudgets_wrapsUpstreamErrorAsUpstreamException)

[UNIT] TransactionClientTest — 3 test
   - trend_unwrapsEnvelope_relaysJwt_andSendsDateRange
   - trend_nullData_returnsEmptyList
   - trend_wrapsUpstreamErrorAsUpstreamException

[UNIT] UserClientTest — 3 test  (gRPC client)
   - me_returnsProfile_andRelaysBearerTokenAsMetadata
   - me_returnsNull_whenProfileNotFound
   - me_wrapsServerErrorAsUpstreamException

[UNIT] JwtServiceTest — 5 test  (bộ chuẩn)
[UNIT] CorrelationIdFilterTest — 4 test  (bộ chuẩn correlation id — xem GHI CHÚ CUỐI)
[COMPONENT] OpenApiDocsTest — 1 test / PrometheusEndpointTest — 1 test
[CONTEXT]   DashboardApplicationTests — 1 test

[INTEGRATION] DashboardApiSecurityIntegrationTest — 4 test
   - rejectsRequestWithoutJwt / rejectsJwtWithInvalidSignature / rejectsExpiredJwt
   - validJwtPassesSecurityAndReachesUpstreamFanOut


================================================================================
7) RISK-SERVICE  (rule rủi ro + insight hành vi + phát hiện bất thường + khoản định kỳ)
================================================================================

[UNIT] RiskRuleEngineTest — 24 test  (rule engine phát hiện rủi ro chi/thu)
   - highAmountExpenseFires
   - belowAllThresholdsFiresNothing
   - rapidSpendingFiresOnTheFifthInWindow
   - rapidSpendingDoesNotRefireAfterTheFifth
   - largeDailySpendFiresOnTheCrossingEvent
   - largeDailySpendDoesNotRefireOncePastThreshold
   - multipleRulesCanFireForOneEvent
   - highAmountIncomeFires
   - ordinaryIncomeFiresNothing
   - rapidIncomeFiresOnTheFifthInWindow
   - largeDailyIncomeFiresOnTheCrossingEvent
   - incomeSpikeFiresAtThreeTimesTheUsersOwnMean
   - incomeSpikeNeedsEnoughHistoryToHaveAMean
   Nhóm "ngưỡng theo từng người" (thay ngưỡng cố định dùng chung — 9 test):
   - aHeavySpenderIsJudgedAgainstTheirOwnMeanNotAFlatTenMillion
     (nền 5tr × 12 giao dịch → ngưỡng 25tr; khoản 15tr vượt mốc cứng 10tr cũ mà KHÔNG bắn)
   - andFiresOnceThatHigherBarIsPassed
   - aLightSpenderIsAlertedWellBelowTheFlatThreshold
     (nền 300k → ngưỡng 1,5tr; khoản 2tr bắn — mốc cứng 10tr sẽ không bao giờ bắn cho người này)
   - theFloorStopsATinyBaselineFromAlertingOnACupOfCoffee (sàn = mốc cứng ÷ 10)
   - tooLittleHistoryFallsBackToTheFlatThreshold (<10 quan sát → dùng mốc cứng, người mới
     vẫn được bảo vệ ngay từ giao dịch đầu)
   - theAlertBarSitsAboveTheAnomalyDetectorsThreeTimes
     (3× là địa hạt của anomaly UNUSUAL_TRANSACTION_AMOUNT; risk alert dùng 5× nên
      hai thứ không thành một luật đội hai tên)
   - largeDailySpendUsesTheUsersOwnDailyMean (trung bình tổng-theo-ngày của chính user)
   - theDailyBarNeedsEnoughDaysBeforeItReplacesTheFlatOne
   - incomeIsScaledToThePersonToo
   - transferIsNotEvaluatedOrRecorded (TRANSFER không tính là thu/chi)
   - duplicateEventIsSkipped

15 test cũ pass NGUYÊN VẸN không sửa dòng nào: mock trả null cho baseline nên engine rơi
về mốc cứng — tức chính hợp đồng cold-start. Đã chứng minh 9 test mới bắt được lỗi: ép
`barFrom` luôn trả mốc cứng → đỏ đúng 5 test khẳng định ngưỡng đã dịch chuyển.

[UNIT] InsightServiceTest — 13 test  (insight hành vi, ngưỡng chính xác)
   - spendingIncreaseFiresAtExactly30Percent
   - spendingIncreaseDoesNotFireBelow30Percent
   - spendingIncreaseNeedsAPreviousBaseline
   - categorySurgeFiresAtOrAbove50Percent
   - categorySurgeDoesNotFireBelow50Percent
   - budgetRiskFiresWhenUtilizationExceeds80Percent
   - budgetRiskDoesNotFireAtExactly80Percent
   - lowSavingsRateFiresWhenExpensesReach80PercentOfIncome
   - lowSavingsRateDoesNotFireBelowThreshold
   - lowSavingsRateNeedsPositiveIncome
   - incomeEventIsRecordedButProducesNoInsight
   - ignoresNonExpenseEvents
   - multipleInsightsCanFireForOneEvent

[UNIT] AnomalyServiceTest — 4 test  (phát hiện giao dịch bất thường)
   - firesWhenAmountIsAtLeastThreeTimesAverageWithEnoughHistory
   - doesNotFireWhenHistoryIsInsufficient
   - doesNotFireBelowThreeTimesAverage
   - ignoresNonExpenseEvents

[UNIT] RecurringDetectorTest — 12 test  (nhận diện khoản chi lặp lại — Phase G.1)
   Mở series:
   - opensASeriesWhenASimilarChargeWasOneMonthAgo
   - aMonthlyGapIsStillMonthlyAtTwentyEightAndThirtyOneDays
     (tháng dương lịch dài 28–31 ngày, ngày thu tiền còn trôi qua cuối tuần — dung sai ±5
      là điều kiện để một subscription thật được nhận ra)
   - opensNothingWhenTheGapMatchesNoCadence
   - opensNothingWithoutAnEarlierChargeOfASimilarAmount
   - ignoresAChargeWithNoCategoryOrCurrency
   Tiếp diễn series:
   - confirmsTheSeriesOnTheThirdCharge (2 lần có thể là trùng hợp, 3 lần mới là quy luật)
   - doesNotReconfirmOnTheFourthCharge
   - reportsAPriceRiseOnAnEstablishedSeries
     (11.50 = 1.15× giá đã chốt, VẪN nằm trong dải khớp 25% — đó chính là lý do hai con số
      này khác nhau: nếu dải khớp hẹp hơn ngưỡng tăng giá thì khoản tăng giá sẽ rơi ra
      ngoài series của chính nó và âm thầm mở series thứ hai thay vì được báo)
   - treatsSmallDriftAsTheSamePrice
   - rebasesSilentlyWhenTheChargeGetsCheaper (rẻ đi không phải rủi ro, nhưng phải cập nhật
     giá nếu không mọi khoản sau đó đều trông như một cú giảm)
   - doesNotCountAChargeThatArrivedOffCadence
   - doesNotSeedASecondSeriesWhenOneMatchedOnPriceButNotOnCadence
     (nếu không, hai series sẽ tranh nhau khoản kế tiếp)

[UNIT] RecurringSweeperTest — 3 test  (khoản định kỳ KHÔNG tới — rule duy nhất chạy bằng
   scheduler, vì tín hiệu của nó là một sự VẮNG MẶT, không sự kiện nào được phát ra)
   - asksOnlyForEstablishedSeriesOverdueByMoreThanTheGrace (ân hạn 3 ngày)
   - reportsAnOverdueChargeAgainstItsLastTransaction (không có giao dịch kích hoạt nên
     alert trỏ vào khoản đáng lẽ phải lặp lại)
   - lapsesTheSeriesSoItIsReportedOnlyOnce (không thì mỗi giờ lại báo một lần mãi mãi)

[UNIT] RiskEventConsumerTest — 6 test / BudgetEventConsumerTest — 3 test
   (2 test mới: carriesTheConsumedCorrelationIdOntoTheRiskEvent,
    addsNoCorrelationHeaderWhenTheMdcHasNoId)
[UNIT] KafkaConsumerConfigTest — 2 test  (metric thất bại consumer)
[UNIT] CorrelationIdFilterTest — 4 test  (bộ chuẩn correlation id — xem GHI CHÚ CUỐI)
[UNIT] CorrelationIdRecordInterceptorTest — 5 test  (correlation id qua Kafka — xem GHI CHÚ CUỐI)

[INTEGRATION] RiskDetectionIntegrationTest — 4 test  (Kafka + MySQL)
   - highAmountExpensePublishesRiskDetected
   - normalExpenseProducesNoRiskEvent
   - rapidSpendingFiresOnFifthExpenseWithinWindow
   - largeDailySpendFiresWhenDailyTotalExceedsThreshold

[INTEGRATION] InsightApiIntegrationTest — 7 test
   - spendingIncreaseIsGeneratedAndListed
   - categorySurgeFiresWhileUserTotalDoesNot
   - budgetRiskFiresWhenUtilizationExceeds80Percent
   - lowSavingsRateIsGeneratedAndListed
   - lowSavingsRateDoesNotFireBelowThreshold
   - listIsEmptyWhenNoInsights
   - generatedMetricIsExposedPerType

[INTEGRATION] AnomalyApiIntegrationTest — 4 test
   - unusualAmountIsDetectedAndListed
   - doesNotDetectWhenHistoryIsInsufficient
   - listIsEmptyWhenNoAnomalies
   - detectedMetricIsExposed

[INTEGRATION] RecurringApiIntegrationTest — 4 test  (Phase G.1, MySQL thật)
   Tồn tại để phủ đúng hai thứ unit test không với tới: JPQL (dải giá viết theo hệ số của
   chính series, và truy vấn tìm khoản trước đó là truy vấn 1 dòng có sắp xếp) và migration
   V10 mà Hibernate validate đối chiếu.
   - threeMonthlyChargesOpenConfirmAndListASeries
   - aPriceRiseStillMatchesItsOwnSeries
   - aWeeklyGroceryRunDoesNotBecomeAMonthlySeries (các dải chu kỳ không chồng lấn)
   - listIsEmptyWhenNothingRecurs

[INTEGRATION] RiskAlertApiIntegrationTest — 4 test  (read API cảnh báo rủi ro)
[INTEGRATION] InsightAnomalyKafkaE2EIntegrationTest — 2 test
   - transactionCreatedFlowGeneratesInsightAndAnomaly (E2E: sự kiện → insight + anomaly)
   - consumerLagMetricIsExposedForPrometheus


================================================================================
8) NOTIFICATION-SERVICE  (thông báo trong app từ RiskDetected + narrator LLM tùy chọn
   + kênh gửi ra: web push / email / webhook có chữ ký, và digest gom theo user)
================================================================================

[UNIT] NotificationServiceImplTest — 12 test
   - stampsAnImmediateUsersNotificationAsAlreadyAccountedFor
     (digested_at NULL phải nghĩa là "còn nợ một lần gửi"; để NULL cho user IMMEDIATE
      thì scheduler sẽ gửi lại toàn bộ lần thứ hai)
   - leavesADigestUsersNotificationPendingAndSkipsTheContentChannels
     (email/webhook để scheduler lo; web push vẫn bắn ngay vì không mang nội dung)
   - aFailingChannelDoesNotFailTheConsumer
   - createFromEventPersistsNotificationAndInboxRowForNewEvent
   - createFromEventSkipsWhenAlreadyProcessed
   - createFromBudgetExceededSpellsOutTheOverspendUsingBudgetServicesOwnFigures
     (type BUDGET_EXCEEDED, severity HIGH, số tiền nhóm hàng nghìn và độc lập locale;
      nêu thẳng phần vượt thay vì bắt người đọc tự trừ hai số)
   - createFromBudgetExceededIsGuardedByTheSameInbox
   - createFromMonthlyReportStatesTheMonthsFiguresAsAnalyticsComputedThem
     (type MONTHLY_REPORT, severity LOW — bản tổng kết không có gì "sai", không được
      nhuộm màu chuông như một cảnh báo)
   - createFromMonthlyReportOmitsTheCategorySentenceWhenThereWasNoSpend
   - createFromMonthlyReportIsGuardedByTheSameInbox
   - markReadThrowsWhenNotFoundForUser
   - markReadSetsReadFlagWhenFound

[UNIT] NotificationPreferenceServiceImplTest — 8 test  (email / webhook / digest mode)
   - defaultsToImmediateForAUserWhoNeverChose
   - mintsASecretWhenAWebhookUrlIsFirstSet
   - keepsTheSecretWhenOnlyTheSwitchMoves
     (receiver đã cấu hình khoá đó; xoay khoá khi user chỉ tạm tắt là âm thầm phá
      tích hợp của họ)
   - mintsAFreshSecretWhenTheDestinationChanges
   - clearingTheUrlClearsTheSecretAndTheSwitch
   - refusesAUrlTheValidatorRejects
   - changingDigestModeStartsAFreshWindow
     (không mang cái đang chờ sang chế độ mới: đi tới IMMEDIATE thì không còn ai
      flush, đi từ IMMEDIATE thì gửi lại thứ đã gửi)
   - reSelectingTheSameModeTouchesNothingPending

[UNIT] RiskDetectedConsumerTest — 4 test
   - createsNotificationAndCountsCreated
   - countsDuplicateWhenInboxHit
   - ignoresEventWithoutEventId
   - ignoresEventWithoutUserId

[UNIT] TemplateNarratorTest — 5 test  (diễn đạt cảnh báo bằng template)
   - mapsKnownRiskTypeToSpecificWording
   - everyRiskTypeTheRuleEngineEmitsHasItsOwnWording
     (chốt cả 10 rule: một rule mới mà tới tay người dùng dưới dạng "Risk alert" chung chung
      thì tệ hơn là không có — nó báo rằng có chuyện xảy ra mà không nói chuyện gì, và triệu
      chứng duy nhất là một thông báo mơ hồ chẳng ai đi báo lỗi)
   - namesWhatChangedAboutARecurringCharge
   - fallsBackForUnknownRiskType
   - handlesNullRiskTypeWithoutThrowing

[UNIT] MonthlyReportConsumerTest — 4 test  (Phase G.2 — cùng hợp đồng với 2 consumer kia:
   mọi sự kiện rơi vào đúng một trong created / duplicate / ignored)
   - createsNotificationAndCountsCreated
   - countsDuplicateWhenInboxHit
   - ignoresEventWithoutEventId
   - ignoresEventWithoutUserId

[UNIT] LlmAlertNarratorTest — 4 test  (diễn đạt bằng LLM, có fallback template)
   - usesLlmTitleAndMessageOnSuccess
   - fallsBackToTemplateOnHttpError
   - fallsBackToTemplateWhenApiUnreachable
   - fallsBackToTemplateOnEmptyLlmFields

[UNIT] KafkaConsumerConfigTest — 2 test / JwtServiceTest — 5 test
[UNIT] CorrelationIdFilterTest — 4 test  (bộ chuẩn correlation id — xem GHI CHÚ CUỐI)
[UNIT] CorrelationIdRecordInterceptorTest — 5 test  (correlation id qua Kafka — xem GHI CHÚ CUỐI)

[INTEGRATION] NotificationApiCrudIntegrationTest — 4 test  (MySQL)
   - listsNotificationsNewestFirstAndReportsUnreadCount
   - markReadFlipsOneAndDecrementsUnreadCount
   - markAllReadClearsUnread
   - unreadOnlyFilterExcludesReadNotifications

[INTEGRATION] NotificationApiSecurityIntegrationTest — 3 test
   - rejectsRequestWithoutToken
   - rejectsExpiredToken
   - cannotMarkAnotherUsersNotificationRead

[UNIT] VapidSignerTest — 5 test  (header Authorization của web push, RFC 8292)
   - producesAVapidHeaderCarryingTheConfiguredPublicKey
   - signsATokenTheMatchingPublicKeyCanVerify
     (verify bằng nửa public — khoá bị cắt cụt VẪN ký được, chỉ assert này bắt được)
   - scopesTheTokenToThePushServiceOriginNotTheFullEndpoint
   - writesTheAudienceAsAStringBecauseFcmRejectsAOneElementArray
     (hồi quy đo được trên FCM thật: aud chuỗi → 201, aud mảng → 403 "permission
      denied: invalid aud claim". Parse lại thì cả hai đều thành Set nên assert
      trên Claims không phân biệt được — test này đọc JSON thô của payload)
   - failsLoudlyOnATruncatedKeyInsteadOfSigningWithIt
     (hồi quy: gen-vapid-keys.sh từng rụng octet cuối → 31 byte; triệu chứng duy nhất
      lẽ ra là push service trả 401 trên prod)

[UNIT] WebPushChannelTest — 3 test
   - doesNothingWithoutAVapidKeypair
   - skipsUsersWithNoSubscribedBrowser
   - keepsTheSubscriptionWhenThePushServiceIsSimplyUnreachable
     (chỉ 404/410 mới nghĩa là "mất hẳn"; xoá row vì lỗi mạng tạm thời là âm thầm huỷ
      đăng ký của user vĩnh viễn)

[UNIT] EmailChannelTest — 7 test
   - sendsToTheStoredAddressWhenTheUserOptedIn
   - aDigestOfSeveralAlertsIsOneEmailThatListsThem
     (một alert vẫn giữ nguyên title làm subject → đường tức thì đọc y như trước khi
      có digest; chỉ lô thật mới tự xưng là lô)
   - summarisesTheTailOfAPathologicalBurst  (25 alert → liệt kê 20 + "and 5 more")
   - staysSilentWhenNoMailServerIsConfigured
   - doesNotEmailAUserWhoHasNotOptedIn
   - doesNotEmailWhenTheAddressIsMissing
   - swallowsSmtpFailuresSoTheKafkaListenerNeverReplays

[UNIT] WebhookUrlValidatorTest — 10 test  (BIÊN GIỚI CHỐNG SSRF — xem GHI CHÚ CUỐI)
   - acceptsAPublicHttpsUrl
   - rejectsPlainHttp        (một luật này cũng loại luôn mọi service nội bộ chạy http)
   - rejectsLoopback
   - rejectsPrivateRanges    (10/8, 172.16/12, 192.168/16)
   - rejectsTheCloudMetadataAddress  (169.254.169.254 — link-local, lọt qua check chỉ
     biết RFC 1918)
   - rejectsCarrierGradeNat  (100.64/10 — isSiteLocalAddress() KHÔNG bắt)
   - rejectsIpv6UniqueLocalAndLoopback  (fc00::/7 cũng không bị isSiteLocalAddress bắt)
   - rejectsCredentialsInTheUrl
   - rejectsGarbageAndHostlessUrls
   - isAllowedReportsRatherThanThrows
   (toàn IP literal nên không phụ thuộc DNS, chạy offline vẫn xanh)

[UNIT] WebhookSignerTest — 4 test  (HMAC-SHA256, lược đồ Stripe/GitHub)
   - signsTheTimestampAndBodyTogether  (tự tính lại HMAC, không ghim chuỗi vàng)
   - coversTheTimestampSoItCannotBeRewritten
     (nếu MAC chỉ trùm body thì request bắt được là token replay vĩnh viễn)
   - adifferentSecretProducesADifferentSignature
   - mintsPrefixedSecretsThatDoNotRepeat  (49 ký tự, vừa VARCHAR(64) của V4)

[UNIT] WebhookChannelTest — 8 test  (HTTP thật qua JDK HttpServer, không thêm thư viện)
   - postsEveryAlertInTheBatchAsOneSignedCall
   - signsExactlyTheBytesItSent
     (serialize 1 lần rồi ký chính bytes đó; serialize lần hai có thể lệch và mọi
      chữ ký thành sai)
   - wrapsASingleAlertInTheSameArrayShapeAsABatch
     (receiver chỉ phải viết 1 parser; chuyển user sang digest không phá tích hợp)
   - staysSilentWhenTheUserHasNoWebhook
   - staysSilentWhenTheStoredUrlNoLongerValidates  (DNS bị trỏ lại sau khi lưu)
   - doesNotFollowRedirects  (302 là cách rẻ nhất để lách validator)
   - swallowsReceiverFailuresSoTheKafkaListenerNeverReplays
   - swallowsAnUnreachableReceiver

[UNIT] DigestSchedulerTest — 8 test  (gom cảnh báo theo cửa sổ của từng user)
   - holdsTheDigestBackUntilTheWindowHasElapsed
   - sendsOneDeliveryCoveringTheWholeWindow
   - theWindowIsTheOneTheUserChose  (cùng tuổi 70 phút: HOURLY tới hạn, DAILY chưa)
   - leavesTheImmediateChannelsToTheCreatePath  (không đẩy push lần hai)
   - stampsExactlyTheRowsItSent
     (đóng dấu theo id, không theo "mọi thứ đang chờ của user này" — alert tới giữa
      chừng sẽ bị đánh dấu đã gửi mà chưa từng gửi)
   - stampsButDoesNotResendStragglersLeftByAUserOnImmediate
   - oneBrokenUserDoesNotStopEveryoneElse
   - aChannelThatThrowsDoesNotStrandTheStamp
     (đóng dấu TRƯỚC khi gửi: gửi trước rồi đóng dấu thì một receiver hỏng biến thành
      cùng một digest gửi lại mỗi 5 phút mãi mãi)

[INTEGRATION] OpenApiDocsIntegrationTest — 1 test
[INTEGRATION] PrometheusEndpointIntegrationTest — 1 test


================================================================================
9) ANALYTICS-SERVICE  (mô hình đọc CQRS + tóm tắt AI tùy chọn + báo cáo tháng
                       + mô hình dự báo chi tiêu đã huấn luyện)
================================================================================

[UNIT] AnalyticsServiceImplTest — 8 test
   - overviewComputesTotalsSavingsRateAndExpenseChange
   - forecastForPastMonthProjectsToActual  (tháng đã xong ⇒ luôn RUN_RATE)
   - forecastFallsBackToTheRunRateWhenNoModelHasBeenTrained
     (trạng thái của MỌI tài khoản cho tới khi trainer chạy, và của cả nền tảng khi cờ tắt)
   - forecastUsesTheTrainedModelForTheDaysStillToCome
     (mô hình phẳng 10/ngày ⇒ 300 + 10 × số ngày còn lại — con số run-rate KHÔNG thể tạo ra,
      vì nó sẽ nhân 300 theo tỷ lệ ngày đã trôi. `assumeTrue` bỏ qua vào ngày cuối tháng:
      lúc đó không còn ngày nào để dự báo nên run-rate mới là câu trả lời đúng)
   - forecastKeepsTheRunRateWhenTheModelLostItsBacktest
     (model_mae = baseline_mae ⇒ hoà, mà hoà thì thuộc về cách đơn giản hơn)
   - forecastKeepsTheRunRateWhileAModelIsStillUnvalidated
     (chưa đủ lịch sử để chấm điểm ⇒ 3 cột NULL; "chưa biết" không phải bằng chứng cải thiện)
   - categoriesComputeShareWithinType
   - summaryUsesTemplateAndIsNotAiGenerated

[UNIT] SeasonalTrendTrainerTest — 11 test  (số học thuần, KHÔNG cần Docker/Spring)
   Mỗi test dựng một chuỗi có hình dạng biết trước rồi khẳng định mô hình khôi phục lại
   đúng hình dạng đó — một mô hình chỉ "sinh ra vài con số" sẽ qua smoke test và trượt cả 11.
   - recoversWeeklyShape  (cuối tuần gấp đôi ngày thường ⇒ tỷ lệ chỉ số = 2,0)
   - predictionCarriesTheWeeklyShape  (thứ Bảy > thứ Hai — điều run-rate không nói được)
   - recoversTrend  (dốc 2/ngày ⇒ trend ≈ 2,0 và ngoại suy 10 ngày ≈ 152)
   - flatSeriesGivesNeutralIndices
   - refusesShortSeries  (< 14 ngày ⇒ null, không đoán bừa)
   - refusesAllZeroSeries
   - predictionIsFlooredAtZero  (dốc xuống mạnh vẫn không ra chi tiêu âm)
   - shrinkageFallsBackToThePopulation  (chưa có bằng chứng riêng ⇒ lấy hình dạng của đám đông)
   - shrinkageYieldsToTheUserAsEvidenceGrows
   - shrinkageIsProportionalToEvidence  (4 quan sát = trọng số prior ⇒ đúng giữa)
   - countsWeekdayObservations
   📌 `shrinkTowards` CHUẨN HOÁ kết quả về trung bình 1, nên vector kỳ vọng trong test phải
      sẵn trung bình 1 — lần đầu viết sai chỗ này và test đỏ, không phải code sai.

[UNIT] HoldoutBacktestTest — 5 test  (số học thuần, KHÔNG cần Docker/Spring)
   Chấm điểm mô hình trên 14 ngày nó CHƯA từng thấy, đối đầu trực tiếp với run-rate.
   Các ca đáng giá là những ca mô hình THUA — một backtest chỉ biết gật đầu thì vô dụng.
   - modelWinsOnSeasonalSpending  (chuỗi có hình dạng tuần thật ⇒ MAE mô hình < MAE run-rate)
   - modelLosesWhenTheTrendBreaks  (học dốc lên rồi chi tiêu sụp ⇒ ngoại suy sai nặng hơn
     đường phẳng; đây chính là ca mà cái gate sinh ra để chặn)
   - aTieIsNotAWin  (chuỗi phẳng: cả hai đều đúng tuyệt đối ⇒ không phục vụ mô hình)
   - trainingStopsAtTheSplit  (train phẳng 100, holdout nhảy 1000 ⇒ MAE > 800; nếu fit có
     "nhìn trộm" holdout thì level đã kéo lên và điểm số sẽ đẹp giả tạo — test rò rỉ dữ liệu)
   - refusesWhenThereIsNothingLeftToTrainOn  (cắt 14 ngày mà phần còn lại < 14 ⇒ null)

[UNIT] ForecastModelMetricsTest — 4 test  (đo mô hình bằng dữ liệu thật, không phải bằng log)
   - reportsEachOutcome  (10 mô hình = 3 serving + 2 unvalidated + 5 beaten; `beaten` là suy ra
     nên 3 nhóm luôn cộng đúng tổng)
   - usesTheServingThreshold  (bắt tận tay tham số margin gửi xuống repository = 1 −
     REQUIRED_IMPROVEMENT; gauge đếm "thắng" theo luật KHÁC luật phục vụ thì còn tệ hơn không có)
   - reportsNaNBeforeAnythingIsScored  (chưa chấm điểm ⇒ NaN, không phải 0 — 0 sẽ vẽ thành
     "mô hình chính xác vô hạn", đúng ngược ý nghĩa)
   - survivesARepositoryFailure  (DB chết ⇒ giữ giá trị cũ, không ném; metric không được phép là
     lý do một sweep hay một lần khởi động thất bại)

[UNIT] ModelTrainingSchedulerTest — 2 test  (sweep hằng đêm)
   - trainsAndRefreshes  (huấn luyện tới HÔM QUA + đọc lại gauge sau sweep)
   - survivesAFailedSweep  (sweep lỗi ⇒ đếm vào counter, KHÔNG ném ra — ném thì chết luôn thread
     scheduler và không còn lần retry nào; và không refresh gauge vì chẳng có gì được ghi)

[UNIT] SpendingModelTrainerTest — 2 test  (dây nối giữa fit, chấm điểm và lưu)
   - writesTheAccuracyItMeasured  (holdout_days/model_mae/baseline_mae ghi cạnh đúng fit đã
     sinh ra chúng, và fit đó học được cuối tuần đắt hơn)
   - keepsFittingSeparateFromServing  (chuỗi phẳng vẫn được LƯU mô hình nhưng
     `beatsRunRate()` = false — "fit được" và "đáng phục vụ" là hai quyết định khác nhau)

[UNIT] RollupServiceTest — 6 test  (dựng read-model từ TransactionCreated)
   - createsNewRollupForFirstEventInSlot
   - addsToExistingRollupInSlot
   - skipsDuplicateEvent (idempotent)
   - usesUncategorizedSentinelWhenCategoryMissing
   - foldsTheSameEventIntoTheDailySeriesAsWell  (chuỗi ngày là thứ mô hình học)
   - skipsTheDailySeriesForADuplicateEventToo  (cùng một inbox, không lệch nhau được)

[UNIT] LlmSummarizerTest — 4 test  (tóm tắt tháng bằng LLM, fallback template)
   - usesLlmSummaryOnSuccess
   - fallsBackToTemplateOnHttpError
   - fallsBackToTemplateWhenApiUnreachable
   - fallsBackToTemplateOnEmptySummary

[UNIT] TemplateSummarizerTest — 2 test
   - describesSavingsRateTrendAndTopCategory
   - reportsNoActivityWhenEmpty

[UNIT] MonthlyReportSchedulerTest — 5 test  (Phase G.2 — sự kiện đầu tiên service này PHÁT)
   - publishesOneReportPerUserWithActivity
   - skipsAUserWhoAlreadyHasThatMonthsReport (bảng monthly_report_sent tồn tại vì quét chạy
     HÀNG NGÀY; chỉ lần quét đầu của tháng được phép thành một cái email)
   - publishesNothingForAMonthWithNoActivity
   - carriesTheFiguresAndTheLargestExpenseCategory (danh mục CHI lớn nhất, không phải danh
     mục biến động mạnh nhất — "top movers" của overview xếp theo mức thay đổi nên danh mục
     to nhất thường không nằm trong đó)
   - recordsTheReportBeforePublishingIt (crash ở giữa mất 1 báo cáo của 1 người; thứ tự
     ngược lại sẽ gửi lại cho TẤT CẢ, mỗi ngày, cho tới khi thành công)

[UNIT] CorrelationIdFilterTest — 4 test  (bộ chuẩn correlation id — xem GHI CHÚ CUỐI)
[UNIT] CorrelationIdRecordInterceptorTest — 5 test  (correlation id qua Kafka — xem GHI CHÚ CUỐI)

[INTEGRATION] MonthlyReportPersistenceIntegrationTest — 3 test  (MySQL thật — integration
   test ĐẦU TIÊN của service này). Khởi động context là mục đích chứ không phải tác dụng
   phụ: Flyway áp mọi migration rồi Hibernate `validate` đối chiếu entity, nên schema lệch
   mapping sẽ đỏ ở đây thay vì lúc khởi động trên prod.
   - recordsAndRecognisesAReportAlreadySent
   - refusesASecondReportForTheSameUserAndMonth (ràng buộc UNIQUE là lưới cuối: một email
     đã gửi thì không rút lại được)
   - findsEachUserWithActivityInTheMonthOnce (truy vấn duy nhất trong service cắt ngang
     nhiều user — nó là job theo lịch, không có JWT nào để scope theo)

[INTEGRATION] SpendingModelAccuracyIntegrationTest — 3 test  (MySQL thật — mock repository chỉ
   chứng minh số học trong Java, KHÔNG chứng minh JPQL chạy được, cũng không nói gì về cách
   NULL và phép so `model_mae < baseline_mae * 0.95` hành xử trong DB)
   - countsWinnersLosersAndUnscored  (4 dòng: thắng đậm / hơn 4% (THUA vì dưới margin 5%) /
     thua / chưa chấm. Cái 4% cố ý đặt sát biên — đây là chỗ cả cái gate xoay quanh)
   - averagesTheErrorRatio  (chỉ tính trên dòng đã chấm điểm)
   - returnsNullWithoutScores  (null chứ không phải 0 ⇒ gauge ra NaN ⇒ panel để trống, thay vì
     khoe "mô hình không có sai số")

[INTEGRATION] ForecastModelMetricsIntegrationTest — 2 test  (gauge có THẬT lúc runtime không)
   Mọi thứ ở metric này đều có điều kiện: bean chỉ tồn tại khi bật cờ, giá trị seed từ
   `ApplicationReadyEvent`, tên meter là chuỗi ký tự — cả ba đều hỏng âm thầm được và để lại
   một dashboard trống mà không ai biết vì sao. Test bật cờ, khởi động context thật rồi đọc
   ngược registry.
   - registersTheGauges  (3 outcome + error ratio; đồng thời chứng minh gauge
     `finsight.analytics.forecast.models` và counter `…models.trained` cùng tồn tại được)
   - countsWhatIsInTheTable  (ghi 3 dòng thật → refresh → 1/1/1)


================================================================================
10) FRONTEND `web/` (React + Vite) — VITEST: LOGIC THUẦN + COMPONENT/HOOK
================================================================================
Chạy bằng `npm test --prefix web` (= `vitest run`), không cần Docker, không cần
backend. Nằm trong CI ở `.github/workflows/frontend.yml`. Môi trường `jsdom`
(một project duy nhất cho cả hai loại), setup ở `src/test/setup.ts` (jest-dom
matchers + cleanup sau mỗi test vì Vitest chạy không có globals).
Test trình duyệt thật nằm ở mục 11.

[UNIT] src/lib/format.test.ts — 11 test  (định dạng tiền và input tiền)
   - money: formats a USD amount with symbol, thousands and 2 decimals
   - money: honours a non-default currency code
   - money: falls back to "<amount> <currency>" when the currency code is invalid
   - categoryName: returns the matching category name
   - categoryName: falls back to "#<id>" for an unknown id
   - groupThousands: dot-separates thousands
   - groupThousands: strips non-digits before grouping
   - groupThousands: returns empty for a digit-free string
   - sanitizeMoneyInput: keeps digits only
   - sanitizeMoneyInput: caps the length at MAX_MONEY_DIGITS
     (soi gương @Digits(integer=15) phía backend)
   - sanitizeMoneyInput: returns empty when there are no digits

[UNIT] src/lib/csv.test.ts — 26 test  (đọc sao kê CSV: dấu phân cách, số tiền, ngày)
   - detectDelimiter: picks the comma for a comma-separated header
   - detectDelimiter: picks the semicolon used by non-en-US exports
   - detectDelimiter: ignores delimiters inside a quoted field
   - parseCsv: splits rows and cells
   - parseCsv: keeps a delimiter that is inside quotes
   - parseCsv: unescapes a doubled quote
   - parseCsv: handles CRLF line endings and a trailing newline
   - parseCsv: strips a UTF-8 BOM so the first header is not corrupted
   - parseCsv: keeps a newline that is inside a quoted field
   - parseAmount: reads a plain number
   - parseAmount: reads en-US grouping with decimals
   - parseAmount: reads the other convention, where the comma is the decimal mark
   - parseAmount: treats a lone separator with three digits behind it as grouping
     (cách đọc VND: 1.500 = một nghìn năm trăm)
   - parseAmount: treats a lone separator with two digits behind it as a decimal mark
   - parseAmount: keeps a minus sign, which is how an expense announces itself
   - parseAmount: reads accounting parentheses as negative
   - parseAmount: ignores a currency symbol or code around the number (2 test)
   - parseAmount: returns null when there is no number to read
   - parseDate: reads day-first / month-first / ISO dates (3 test)
   - parseDate: accepts dots and dashes as separators
   - parseDate: expands a two-digit year into this century
   - parseDate: ignores a time that follows the date
   - parseDate: rejects a day that does not exist in that month
   - parseDate: rejects text that is not a date

[UNIT] src/lib/settled.test.ts — 6 test  (nạp trang mà một request hỏng không kéo cả trang xuống)
   Cả 4 trang chính đều dùng `Promise.all` ⇒ **hỏng một là hỏng tất**. Offline, một request
   trượt cache (thường là `listTransactions` vì URL kèm tháng) làm rơi luôn 3 câu trả lời ĐÃ về
   ⇒ ô Category rỗng ⇒ form không dùng được. Người dùng thấy là "app hỏng", không phải "thiếu
   một con số".
   - keeps what arrived / falls back for what did not
   - reports nothing when everything arrived
   - reports a failure while online, exactly as before  (không nới lỏng hành vi online)
   - stays quiet about a partial failure while offline  (banner offline đã giải thích rồi; báo lỗi
     thêm là nói app hỏng trong khi nó đang làm đúng điều đã hứa)
   - still speaks up when offline leaves nothing at all to show

[UNIT] src/lib/outbox.test.ts — 9 test  (hàng đợi ghi offline)
   - keeps a queued write, with a token of its own  (ngày người dùng chọn ĐI CÙNG lệnh ghi —
     nếu không, giao dịch soạn thứ Ba gửi thứ Năm sẽ ghi vào thứ Năm)
   - gives each queued write a distinct token
   - survives a reload, which is the entire point of storing it
   - refuses to grow without bound  (từ chối cái thứ 101 để form còn báo được cho người dùng,
     thay vì giả vờ đã lưu một thứ sẽ không bao giờ gửi)
   - sends oldest first and clears what landed
   - stops at the first failure and keeps the rest queued  (mạng còn hỏng thì gửi tiếp cũng vô
     ích, chỉ tốn pin)
   - drops a write the server will never accept, so it cannot block the queue  (4xx vĩnh viễn bị
     bỏ; 408/429 KHÔNG — hai mã đó nghĩa là "lát nữa", đúng việc của hàng đợi)
   - clears on sign-out, since a queue belongs to the account that filled it
   - treats unreadable storage as an empty queue rather than throwing

[UNIT] src/lib/download.test.ts — 5 test  (khoảng ngày cho nút Xuất CSV)
   - monthRange: covers a whole 31-day month / a 30-day month
   - monthRange: gets February right in a common year and a leap year
     (ngày cuối được TÍNH chứ không gán cứng 28 — gán cứng thì cứ 4 năm lại mất một ngày
      giao dịch, và chỉ mất ở tháng 2)
   - monthRange: returns an empty range for "all months" (không có ngày = không lọc ngày)
   - monthRange: ignores a value that is not YYYY-MM

[UNIT] src/lib/jwt.test.ts — 6 test  (giải mã JWT phía client, CHỈ để dựng UI)
   - decodeJwt: decodes the payload claims
   - decodeJwt: handles base64url payloads containing - and _
   - decodeJwt: returns null for a null token
   - decodeJwt: returns null for a malformed token (no dot / bad base64)
   - roleFromToken: extracts the role claim
   - roleFromToken: returns null when there is no role or no token

[UNIT] src/games/roulette/engine.test.ts — 12 test  (toán chi trả LuckyMe)
   - wheel: has 38 distinct pockets (American: 0, 00, 1..36)
   - colourOf: 0 and 00 are green
   - colourOf: matches the standard red/black assignment
   - colourOf: splits 1..36 into exactly 18 red and 18 black
   - payout: every inside bet type except basket pays (36 - n)/n
   - payout: the basket is the deliberate outlier — 6:1 on 5 pockets, not 6.2:1
   - payout: every even-money bet pays 1:1
   - classify: rejects an illegal chip position (non-adjacent numbers)
   - classify: is order-independent — keyOf canonicalises the cover set
   - classify: an empty selection is not a bet
   - outside bets: each column/dozen covers 12, each even-money bet covers 18
   - probability: is n / 38

Bất biến `(36 - n)/n` ở đây soi gương `RouletteTest` phía transaction-service:
cùng một luật chi trả, kiểm ở cả hai đầu.

[COMPONENT] src/auth/AuthContext.test.tsx — 7 test  (claim → quyền, và lưu token)
   - starts signed out when no token is stored
   - reads the role and email out of a token already in storage
   - lets admins into LuckyMe as well as gamers, and nobody else
   - signIn persists both tokens and re-derives the claims
   - signOut clears both tokens from storage
   - refuses to be used outside the provider
   - treats a malformed token as signed in but role-less rather than crashing

[COMPONENT] src/components/routeGuards.test.tsx — 7 test  (3 guard, chạy qua
AuthProvider thật chứ không stub context — chỗ dễ vỡ chính là ánh xạ claim → quyền)
   - ProtectedRoute: sends a signed-out visitor to the login page
   - ProtectedRoute: lets any signed-in user through
   - AdminRoute: sends a non-admin home rather than to login
   - AdminRoute: lets an admin through
   - LuckyMeRoute: lets a gamer through / lets an admin through too
   - LuckyMeRoute: sends an ordinary user home

[COMPONENT] src/theme/index.test.tsx — 6 test  (chọn theme + phản chiếu lên <html>)
   - follows the OS when nothing has been chosen
   - prefers the saved choice over the OS setting
   - falls back to dark when storage or matchMedia is unavailable
   - toggling flips the theme and persists it
   - mirrors the theme onto <html> so the dark: variant and CSS variables follow
   - refuses to be used outside the provider

[COMPONENT] src/pages/Login.test.tsx — 5 test  (form ↔ auth ↔ router; chỉ mock
lời gọi mạng, phần còn lại là thật)
   - renders the sign-in form
   - submits the typed credentials, stores the tokens and lands on the dashboard
   - shows the API error message and stays on the page when sign-in is rejected
   - treats a 200 without a token as a failed sign-in
     (soi gương: /register của auth-service trả 200 mà KHÔNG có token)
   - disables the submit button while the request is in flight

[COMPONENT] src/components/DeliverySettings.test.tsx — 10 test  (kênh gửi cảnh báo:
web push + email + webhook + digest; quy tắc "ẩn hẳn cái không dùng được" thay vì
hiện rồi vô hiệu — chỉ áp cho push/email, vì webhook và digest không cần server cấu
hình gì nên luôn có)
   - still offers webhook and digest when neither push nor email can deliver
   - hides the push row when the server has no VAPID keypair
   - hides the push row when the probe never got an answer
   - hides the email row when the server cannot send mail
   - explains a browser-level block instead of offering a button that cannot work
   - turns email alerts on and reflects what the server stored
     (hai nút cùng hiện chữ "Turn on" → phân biệt bằng aria-label + aria-pressed,
      chính test này phát hiện ra chúng vốn không phân biệt được)
   - shows the signing secret once, when saving a webhook mints one
     (server chỉ trả đúng response đó; không hiện ở đây là user mất khoá)
   - surfaces the server's reason for refusing a URL
     ("must use https" / "must point at a public address" là chỗ DUY NHẤT luật chống
      SSRF hiện ra với người đang cấu hình)
   - clears the webhook, and the secret with it
   - switches digest mode and marks the chosen one

[HOOK] src/hooks/useWebPush.test.ts — 6 test  (5 kết cục của việc dò khả năng
push; jsdom không có service worker lẫn PushManager nên phải dựng tay)
   - stays unsupported when the browser has no Push API, without asking the server
   - tells a failed probe apart from an unsupported browser, and keeps the cause
     (chính là nợ kỹ thuật ghi ở phiên 2026-08-02: catch cũ gộp mọi lỗi thành
      'unsupported' nên "trình duyệt không hỗ trợ" và "API hỏng" trông giống hệt
      nhau — đã chứng minh test đỏ khi gỡ bản vá ra)
   - reports a server without a VAPID keypair as unconfigured
   - reports a browser-level denial, which the page cannot undo
   - is off without a subscription and on with one
   - keeps the switch on screen when subscribing fails
     ('error' sẽ ẩn mất chính cái nút vừa bấm → thất bại lúc subscribe vẫn về 'off')

[COMPONENT] src/pages/LuckyMe.test.tsx — 1 test  (lối vào game phải bấm được bằng
bàn phím; useAuth mock thành admin vì đồng xu chỉ rigged xanh cho admin)
   - offers each playable game as a real button named after that game
     (hồi quy a11y: card game vốn là `<article onClick>` — không nhận focus, KHÔNG
      nằm trong cây a11y, nên trước bản vá người dùng bàn phím/screen reader không
      mở được game nào. Assert bằng ROLE chứ không bằng text, vì role đúng là thứ
      bàn phím/screen reader với tới được. Tên nút kèm tên game: "Play" lặp trên
      mọi card thì screen reader không biết đang ở card nào)

[COMPONENT] src/pages/Transactions.test.tsx — 8 test  (bất biến category ↔ type
   +3 test 2026-08-21 cho ghi offline — **phát hiện bằng cách dùng app thật trên iPhone ở chế độ
   máy bay, toàn bộ test tự động lúc đó đều xanh**:
   - queues an expense even when no budget data could be loaded
     📌 **Hai cửa chặn, không phải một.** (1) `handleSubmit` bắt buộc EXPENSE phải gắn ngân sách,
        mà offline `budgets` rỗng ⇒ `return` trước khi tới hàng đợi. (2) Tệ hơn: **nút submit
        `disabled` luôn** khi không có ngân sách khớp ⇒ bấm không ra gì, **không lỗi, không dấu
        vết**. Mà EXPENSE là loại MẶC ĐỊNH ⇒ tính năng ghi offline không thể chạm tới trong đúng
        tình huống nó sinh ra để phục vụ. Nay cả hai đều bỏ qua khi offline.
   - gives the queued write the date the user chose, not the day it is sent
   - tells the user it is holding something, rather than going quiet
   - still renders the form when one of the four requests misses the cache
     📌 **Cửa chặn thứ BA**, báo từ điện thoại: "ô Category rỗng, không thêm được gì". Service
        worker CÓ cache categories — chính trang vứt đi, vì `Promise.all` hỏng một là hỏng tất.
   - does not shout an error for a partial load it already explains with a banner
   (cùng lúc: nhánh TRANSFER trước đó **chưa hề nối** vào hàng đợi — sót, không phải chủ ý)
của form; chỉ mock lời gọi mạng)
   - defaults the category to one of the current type
   - moves the category to the new type instead of keeping a contradictory pair
   - still posts a real category when the type changed before the categories arrived
     (hồi quy: đổi type lúc category chưa về thì ô category rỗng, mà value rỗng
      không khớp <option> nào nên trình duyệt VẪN hiển thị option đầu — form trông
      như đã điền nhưng post categoryId 0 và API từ chối)

[COMPONENT] src/pages/Analytics.test.tsx — 2 test  (trang phải nói RÕ con số dự báo
đến từ đâu; chỉ mock lời gọi mạng)
   - says the run rate answered, and shows no band it does not have
     (run-rate không có ước lượng sai số ⇒ bịa ra một khoảng là nói dối)
   - says the model answered, and shows the band it reported
     (hồi quy: chú thích trước đây LUÔN là "at the current pace" — kể cả với con số mà
      tốc độ hiện tại không hề tạo ra. Đã chứng minh test có răng: bỏ điều kiện đi thì
      đúng test này đỏ)

[COMPONENT] src/App.test.tsx — 3 test  (banner phải nằm ĐÚNG CHỖ trong cây route)
   - offers the install on the sign-in screen, where a new visitor actually lands
     📌 **Hồi quy có thật:** banner cài đặt ban đầu đặt trong `Layout`, mà `Layout` chỉ bọc các
        route ĐÃ đăng nhập ⇒ nó vô hình với đúng người nó viết cho: khách lần đầu đang đứng ở
        `/login`. Không lỗi, không log, chỉ đơn giản là không bao giờ render. Người dùng mở
        vernfy.com trên Safari iOS và không thấy gì — đó là cách lỗi này bị phát hiện.
   - still offers it on a signed-in route
   - says nothing once the app already runs from the home screen
   📌 Đã chứng minh có răng: bỏ `<InstallBanner />` khỏi `App` thì 2 test đầu đỏ.

[UNIT] src/hooks/useInstallPrompt.test.ts — 8 test  (luồng mời cài app)
   Trước đây `beforeinstallprompt` KHÔNG được xử lý ở đâu cả: app cài được nhưng không ai
   được báo là cài được. Hook này đóng đúng khoảng đó.
   - offers nothing until the browser says the app is installable
     (Chromium chỉ bắn sự kiện khi nó thấy đủ điều kiện + user đủ tương tác — có thể vài giây
      sau, hoặc KHÔNG BAO GIỜ. Hiện nút trước đó là mở một hộp thoại không tồn tại)
   - offers the real prompt once the event arrives  (kèm khẳng định `defaultPrevented`)
   - opens the browser dialog and then stands down  (sự kiện dùng một lần, Chromium từ chối
     `prompt()` lần hai trên cùng một event)
   - remembers a dismissal, so it never asks twice  (mount lại = tải lại trang)
   - falls back to instructions on iOS, which fires no event at all
   - recognises an iPad, which claims to be a Mac  (iPadOS 13+ khai UA là Macintosh ⇒ phải bắt
     bằng `maxTouchPoints`, không bắt bằng tên)
   - says nothing once the app is already installed  (`navigator.standalone`)
   - drops the offer the moment the install completes  (`appinstalled`)
   📌 **Fixture sai chứ không phải code sai:** `new Event()` mặc định `cancelable: false`, nên
      `preventDefault()` là no-op im lặng và test báo "không chặn được mini-infobar" oan. Sự
      kiện thật của Chromium huỷ được ⇒ fixture phải `{ cancelable: true }`.

[UNIT] src/pwa.test.ts — 8 test  (cài đặt được như app: manifest + icon + index.html)
   - ships a narrow screenshot, which is the one a phone install dialog uses
     (thiếu `screenshots` thì Chromium hạ xuống hộp thoại cài tối giản, **không log gì**; chỉ có
      `wide` mà không có `narrow` thì điện thoại — nơi người ta thực sự cài — cũng mất hộp thoại
      đầy đủ)
   - sends every shortcut to a route the app actually has
     (shortcut trỏ sai route vẫn MỞ được — nginx trả index.html cho mọi đường dẫn — nên người
      dùng rơi vào trang không tồn tại của SPA, và không chỗ nào báo lỗi gõ nhầm)
   - is named .json, the only extension nginx has a MIME type for
     (ảnh nginx phục vụ SPA KHÔNG có entry `.webmanifest` trong mime.types — đã kiểm
      trên container prod, nginx 1.27.5, 0 entry — nên file đó sẽ đi ra dưới
      `application/octet-stream`; `.json` thì đã được ánh xạ sẵn)
   - carries the members a browser requires before it will offer to install
   - offers both icon sizes Chrome asks for, plus a maskable one
   - points every icon at a file that is really there
     (hồi quy: `try_files $uri $uri/ /index.html` khiến file thiếu trả về index.html
      kèm status 200 chứ không phải 404, nên đường dẫn gõ sai biểu hiện thành lỗi
      "manifest parse error" khó hiểu)
   - links the manifest, so the browser ever looks for it
   - ships an apple-touch-icon, the only icon iOS reads for the home screen
   - describes the product, since #root is empty to anything that does not run JS
     (hồi quy: không có `<meta name="description">` nên Google trích ngược chính
      form đăng nhập làm mô tả — "Sign in to Vernfy. Email Password Sign in." — và
      bản tóm tắt của AI kết luận đây là "cổng đăng nhập chung chung, không rõ mục
      đích". Test chốt luôn độ dài ≤ 160 ký tự, quá đó công cụ tìm kiếm cắt bỏ)
   📌 Đọc file bằng `?raw` + `import.meta.glob` của Vite chứ KHÔNG dùng `node:fs`, vì
      `tsconfig.app.json` cố ý chỉ cấp `types: ["vite/client"]` cho `src/`.

[UNIT] src/sw.test.ts — 13 test  (service worker: cache đọc khi mất mạng + bàn giao bản mới)
   +3 test 2026-08-19 cho luồng cập nhật:
   - does not take over on install, however new it is
     (BỎ `skipWaiting()`: chiếm quyền giữa phiên sẽ tráo bundle dưới chân trang đang chạy, route
      lazy-load sau đó xin một chunk mà bản mới không còn ⇒ "Failed to fetch dynamically imported
      module", đúng lúc deploy, trên màn hình người dùng đang mở. **Đã chứng minh test có răng:**
      trả `skipWaiting()` về chỗ cũ thì đúng test này đỏ)
   - hands over only when the page asks it to  (thông điệp `SKIP_WAITING`)
   - ignores a message it does not understand
   Nạp CHÍNH `public/sw.js` bằng `?raw` rồi chạy trong một scope worker giả (`new
   Function` với `self`/`caches`/`fetch` bơm vào), thay vì viết lại logic trong test —
   `sw.js` là file tĩnh không ai import, nên một test mô phỏng lại sẽ vẫn xanh sau khi
   file thật đã hỏng.
   - serves the last good API response when the network is gone
   - fails rather than inventing an answer when nothing was ever cached
   - never caches a write, so an offline edit cannot look like it succeeded
   - leaves auth, notifications, push and game requests alone
     (danh sách cho phép, không phải danh sách chặn: /auth mang token, /notifications và
      /push phải phản ánh server ngay lúc đó, /game do server quyết định — một kết quả
      quay lại từ cache là một lời nói dối)
   - ignores another origin entirely
   - does not store an error response, which would outlive the token that caused it
   - falls back to the cached shell for any route, matching nginx try_files
   - drops the API cache on PURGE, so a sign-out leaves nothing readable behind
   - caches the hashed bundles named in the shell, which the first load never routes
     through it
   - clears caches from an older version on activate
   📌 Đã chứng minh test có răng bằng 2 mutation: mở allowlist thành "mọi /api/v1/*" →
      đỏ đúng test allowlist; bỏ nhánh PURGE → đỏ đúng test purge.

[HOOK] src/hooks/useOnline.test.ts — 3 test  (cờ online cho banner ngoại tuyến)
   - starts from whatever the browser already thinks
   - follows the offline and online events
   - stops listening once unmounted, so a late event cannot set state on a dead component

[COMPONENT] src/pages/Import.test.tsx — 3 test  (bảng preview của trang nhập sao
kê: dòng bị giữ lại phải NÓI RA lý do và phải trông như đang bị giữ lại)
   - says why a row is held back whichever field is unreadable
     (hồi quy: lý do vốn render THAY CHO ô Ngày, nên chỉ lỗi ngày mới hiện được;
      dòng "số dư đầu kỳ" có ngày hợp lệ mà thiếu số tiền thì bị loại im lặng)
   - leaves an unimportable row unticked and locked, matching the count
     (hồi quy: ô tick bám vào `skipped` chứ không bám vào `problem`, nên dòng
      không nhập được vẫn hiện tick — màn hình hứa 7 dòng trong khi nút ghi 5)
   - tells the user a category is missing before one is chosen
     (trường hợp hay gặp nhất: chưa chọn danh mục mặc định thì MỌI dòng đều chưa
      sẵn sàng, mà trước đây không dòng nào nói vì sao)


================================================================================
11) FRONTEND E2E `web/e2e/` — PLAYWRIGHT (CẦN STACK ĐANG CHẠY)
================================================================================
Chạy bằng `npm run test:e2e --prefix web`. KHÔNG tự dựng backend: cần
`docker compose up -d` (hoặc stack ephemeral trong `.github/workflows/staging.yml`)
để `/api` có gateway trả lời. Frontend do `vite preview` phục vụ — tức là **bản
build production**, proxy `/api` → gateway giống hệt vai trò của Caddy trên prod.
Mỗi lần chạy tự đăng ký tài khoản riêng nên không phụ thuộc dữ liệu seed — phần đăng
ký dùng chung ở `e2e/support/account.ts` (không phải file test nên Playwright bỏ qua).
📌 `signUp` chờ `/api/v1/auth/login` trả về **dưới 500** trước khi gửi form. Login bị từ
   chối là kết quả TỐT ở đây: nó chứng minh request đã tới auth-service. Thứ phải chờ
   là **503 "Downstream service is unavailable"** — gateway fail-fast trong lúc
   auth-service còn đang lên. Container báo `healthy` KHÔNG có nghĩa mạch qua gateway đã
   thông, và triệu chứng của nó là form đăng ký im lặng không đi đâu cả, trông y hệt lỗi
   test. Đã tái hiện được bằng `docker compose restart auth-service` rồi chạy ngay.

[E2E] e2e/journey.spec.ts — 3 test  (chromium)
   - a new user can sign up, record income and find it in their history
     (đăng ký → bỏ qua tour → /transactions → tạo INCOME → thấy dòng trong lịch sử
      → dashboard qua BFF cũng thấy)
   - a signed-out visitor is sent to the login page
   - wrong credentials are rejected with the API error, not a blank page

[E2E] e2e/money-loop.spec.ts — 3 test  (chromium)
   Vòng tiền đầy đủ: ví → ngân sách → khoản chi → các con số mọi màn hình khác suy ra
   từ nó. `journey.spec.ts` cố ý ghi INCOME vì thu nhập đứng một mình; khoản CHI mới là
   nửa khó — phải gắn vào một ngân sách do người dùng chọn, phải trừ số dư ví do
   transaction-service giữ, và phải tới budget-service **qua Kafka**.
   - an expense moves the wallet, the budget tally and the dashboard together
     (4 mốc: dòng trong lịch sử → số dư ví giảm đúng → **`spent` của ngân sách cập nhật
      qua Kafka** (poll + reload, hạn 60s: request tạo giao dịch đã trả 200 từ lâu
      trước khi outbox relay phát đi) → dashboard đọc lại qua BFF cũng thấy)
   - an expense past the limit warns at once, before the tally catches up
     (cảnh báo vượt hạn mức tính ở client nên hiện ngay, không chờ Kafka; sau đó
      ngân sách mới thật sự mang nhãn "Over budget" khi sự kiện đã tới)
   - an expense in a category with no budget is refused, not silently recorded
     (chốt lại đúng bất biến của PR #172: mỗi khoản chi trừ đúng 1 ngân sách. Không có
      ngân sách khớp thì trường Budget là ghi chú màu hổ phách chứ không phải select,
      và bấm Add không ghi gì — lịch sử vẫn rỗng, số dư ví không đổi)
   📌 Đây là phần Vitest KHÔNG thay được: mạng giả trả lời tức thì và đúng thứ tự,
      đúng hai tính chất mà đường bất đồng bộ thật không có.

[E2E] e2e/offline.spec.ts — 2 test  (chromium) — **KHÔNG cần backend**
   Spec duy nhất ở mục này chạy được với mỗi `vite preview`: `/login` là trang công
   khai, và điều cần chứng minh là app shell sống sót khi không có server. Vẫn phải là
   bản build production vì service worker cố ý không đăng ký dưới `vite dev`.
   - serves the app shell with the network cut, instead of the browser error page
     (`context.setOffline(true)` cắt mạng thật, không mock fetch)
   - fails offline once the worker is gone, proving it was the worker doing the work
     (đối chứng: gỡ đăng ký worker + xoá cache thì reload offline PHẢI hỏng — không có
      nó, test trên vẫn xanh nếu HTTP cache của Chrome âm thầm trả trang)
   📌 Chính spec này bắt được 2 lỗi mà unit test không thấy: (1) lần tải đầu tiên chưa
      bị worker điều khiển nên bundle `/assets/*` chưa từng được cache → offline ra
      trang trắng; sửa bằng `primeShell()` đọc tên bundle băm từ chính shell HTML.
      (2) `cache.match` trượt vì `Vary` — entry nằm trong cache mà không khớp, app
      trắng và KHÔNG có lỗi nào giải thích; sửa bằng `ignoreVary: true`.

[E2E] e2e/mobile-layout.spec.ts — 1 test  (chromium)
   - no page can be scrolled sideways at phone widths
     (6 route × 2 bề rộng 320/360: `scrollWidth - clientWidth <= 0`, VÀ sau khi
      cuộn ngang thì `window.scrollX` vẫn = 0 — vì chỉ đo scrollWidth thôi thì một
      bố cục bị `clip` che vẫn trông như đạt)
   📌 Phải nằm ở Playwright chứ không thể ở Vitest: **jsdom không có layout engine**,
      không thể biết một phần tử rộng hơn viewport 41px. Đây cũng đúng loại lỗi mà
      device toolbar của devtools báo khác với máy thật, nên nó lọt lên prod.

k6 (`load-test/`) đã phủ đúng những journey này ở tầng API; phần Playwright thêm
vào là nửa mà k6 không thấy: UI có render và nối dây đúng hay không.


================================================================================
GHI CHÚ CUỐI
================================================================================
- Một số lớp base/support không phải test (không tính vào 574): AbstractMySql/
  MockMvc/IntegrationTest, JwtTestTokens, TestUpstreamCalls...
- "BIÊN GIỚI CHỐNG SSRF" (WebhookUrlValidatorTest, notification-service): webhook là chỗ
  DUY NHẤT trong hệ thống mà người dùng chọn địa chỉ rồi **server** đi kết nối tới — từ bên
  trong mạng Docker nơi risk-service trả lời không cần xác thực, còn MySQL/Redis/Kafka gọi
  được bằng tên. Nên các test này không phải kiểm tra hình thức URL: mỗi case là một địa chỉ
  ai đó có thể gõ vào để bắt hệ thống đọc hộ thứ nó không được đọc. Hai luật gánh phần lớn:
  chỉ https, và mọi địa chỉ mà host phân giải ra đều phải định tuyến công khai. Kiểm hai lần
  (lúc lưu và trước mỗi lần gửi, vì DNS trỏ lại được) và tắt follow-redirect ở client.
- Bộ "CorrelationIdFilterTest" (4 test) lặp lại giống nhau ở **cả 9 service**: giữ nguyên
  X-Correlation-ID đến từ upstream, sinh UUID khi thiếu/rỗng, echo lên response, và xoá
  MDC khi request kết thúc (id rò rỉ sẽ dán nhầm sang request kế tiếp trên cùng thread của
  pool). Mỗi service có bản sao filter của riêng nó vì monorepo không có module dùng chung —
  cùng lý do với JwtServiceTest bên dưới.
- Bộ "CorrelationIdRecordInterceptorTest" (5 test) lặp lại ở **4 service consumer**
  (budget, risk, notification, analytics): lấy id từ header X-Correlation-ID của record vào
  MDC, sinh id mới khi header thiếu/rỗng/toàn khoảng trắng, trả record nguyên vẹn, và xoá MDC
  ở afterRecord (thread listener dùng chung — id rò rỉ sẽ dán nhầm sang record kế tiếp).
  Đây là nửa còn lại của CorrelationIdFilter: filter lo hop HTTP, interceptor lo hop Kafka.
- Bộ "JwtServiceTest" (5 test) lặp lại giống nhau ở 5 service (auth, user,
  transaction, budget, dashboard, notification) vì mỗi service tự xác thực token
  cục bộ bằng public key — đây là chủ đích thiết kế (gateway có thể gỡ bỏ, service
  vẫn tự bảo vệ).
- Endpoint /actuator/prometheus và /v3/api-docs được kiểm chứng ở hầu hết service
  để đảm bảo observability + tài liệu API luôn truy cập được.
- Chi tiết điều kiện kích hoạt của rule/insight/anomaly (ngưỡng) xem thêm:
  docs/intelligence.md.
================================================================================
